---
kind: community
id: 3
---
# Community 3 — Community 3

A **Community** of 34 nodes. A **Bridge** links it to others.

## Members
- [[_utils.py]]
- .__init__()
- .format_sizeof()
- .format_interval()
- .status_printer()
- .format_meter()
- ._get_free_pos()
- .__init__()
- Formats a number (greater than unity) with SI Order of Magnitude         prefixe
- Formats a number of seconds as a clock time, [H:]MM:SS          Parameters
- Manage the printing and in-place updating of a line of characters.         Note
- Return a string-based progress bar given some parameters          Parameters
- Skips specified instance.
- Parameters         ----------         iterable  : iterable, optional
- test_format_interval()
- test_format_meter()
- test_si_format()
- Test time interval format
- Test statistics and progress bar formatting
- Test SI unit prefixes
- _is_utf()
- _supports_unicode()
- _is_ascii()
- _environ_cols_wrapper()
- _environ_cols_windows()
- _environ_cols_tput()
- _environ_cols_linux()
- _text_width()
- disp_len()
- disp_trim()
- Return a function which gets width and height of console     (linux,osx,windows,
- cygwin xterm (windows)
- Returns the real on-screen length of a string which may contain     ANSI control
- Trim a string which may contain ANSI control characters.
