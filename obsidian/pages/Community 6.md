---
kind: community
id: 6
---
# Community 6 — Community 6

A **Community** of 26 nodes. A **Bridge** links it to others.

## Members
- keras.py
- TqdmCallback
- .bar2callback()
- .__init__()
- .on_train_begin()
- .on_epoch_begin()
- .on_train_end()
- `keras` callback for epoch and batch progress
- Parameters         ----------         epochs  : int, optional         data_size
- KeyError
- tests_keras.py
- test_keras()
- Test tqdm.keras.TqdmCallback
- ObjectWrapper
- .__getattr__()
- .__setattr__()
- .wrapper_getattr()
- .wrapper_setattr()
- .__init__()
- .__init__()
- .write()
- .__init__()
- Actual `self.getattr` rather than self._wrapped.getattr
- Thin wrapper around a given object
- Encode `s` and pass to the wrapped object's `.write()` method.
- Wrap a given `file`-like object's `read()` or `write()` to report         length
