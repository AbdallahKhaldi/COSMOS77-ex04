---
kind: community
id: 7
---
# Community 7 — Community 7

A **Community** of 25 nodes. A **Bridge** links it to others.

## Members
- tqdm_gui
- std_tqdm
- .__init__()
- .__iter__()
- .update()
- .close()
- .display()
- tgrange()
- Experimental GUI version of tqdm!
- A shortcut for `tqdm.gui.tqdm(xrange(*args), **kwargs)`.     On Python3+, `range
- tqdm_notebook
- .status_printer()
- .display()
- .__init__()
- .__iter__()
- .update()
- .close()
- .moveto()
- .reset()
- tnrange()
- Experimental IPython/Jupyter Notebook widget using tqdm!
- Manage the printing of an IPython/Jupyter Notebook progress bar widget.
- Resets to 0 iterations for repeated use.          Consider combining with `leave
- A shortcut for `tqdm.notebook.tqdm(xrange(*args), **kwargs)`.     On Python3+, `
- .time()
