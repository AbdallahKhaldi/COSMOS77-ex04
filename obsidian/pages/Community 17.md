---
kind: community
id: 17
---
# Community 17 — Community 17

A **Community** of 9 nodes. A **Bridge** links it to others.

## Members
- ._decr_instances()
- .__exit__()
- .__del__()
- .__repr__()
- .close()
- .display()
- Remove from list and reposition other bars         so that newer bars won't over
- Cleanup and (if leave=False) close the progressbar.
- Use `self.sp` to display `msg` in the specified `pos`.          Consider overloa
