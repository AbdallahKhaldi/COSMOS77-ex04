# Copy to `.env` (which is gitignored — CLAUDE.md rule 9) and fill in your own
# FREE Gemini key from https://aistudio.google.com/apikey (no card, no purchase).
# The graph-guided agent (Phase 6) and the naive baseline (Phase 8) read this.
GOOGLE_API_KEY=your_free_gemini_key_here

# Optional alternate provider (config/providers.json -> active: "groq").
# GROQ_API_KEY=your_groq_key_here
