# Prompt log 010 — Phase 10: README lab report + visuals + Spec Sheet

**Phase:** 10 — The §8 README the grader reads first (C10)
**Authors:** Abdallah Khaldi, Tasneem Natour
**Date:** 2026-06-19

## Prompt issued

> Phase 10 goal: the §8 README the grader reads first; every §8 element + visuals
> (`../CLAUDE_CODE_PLAYBOOK.md` §12). Add a `cosmos77-rev run` orchestration
> (prepare-target → graphify → vault → diagrams → agent → fix → compare →
> extensions). ≥250 lines, ≥5 images. Self-Assessment recommending 85 (§15).

## What was done

- **`README.md`** (497 lines, **7** `![` image refs) — all 16 §8 sections,
  grounded in the REAL committed reports (TOKEN_COMPARISON, ARCHITECTURE,
  BUG_ANALYSIS, SPEC_SHEET, IMPACT, ORPHANS) + the PRD research questions, using
  the professor's exact vocabulary. Embeds the four generated PNGs (block diagram,
  OOP schema, knowledge-graph God-Node ego network, token-comparison chart) + a
  Mermaid StateGraph flowchart + the Obsidian graph-view screenshot (the one
  manual image — see `MANUAL_STEPS.md`). Self-Assessment recommends **85** with
  the §15 rationale.
- **`SDK.run()` + CLI `run`** — the end-to-end orchestration
  (prepare-target → graphify → vault → diagrams → agent → fix → compare →
  extensions), unit-tested (every stage invoked once).
- **New visuals** generated from real data: `artifacts/token_comparison.png`
  (the measured 68,336→40,826 tokens / 31→2 files) and
  `artifacts/knowledge_graph.png` (the God Node `tqdm` + its neighbours).
- **`MANUAL_STEPS.md`** — documents the one manual image (the Obsidian graph-view
  screenshot the authors capture in Obsidian Desktop) so the README references
  stay valid.

## Verification

```bash
wc -l README.md                 # 497  (>= 250)
grep -c '!\[' README.md         # 7    (>= 5)
uv run pytest -m "not live" -q  # 190 passed, coverage 98.8%
uv run cosmos77-rev run         # full pipeline (run live in Phase 11 QA)
```

## Notes

- Four of the five README images are generated by the pipeline and committed; the
  fifth (`artifacts/obsidian_graph_view.png`) is a screenshot the authors add via
  Obsidian Desktop before submission (MANUAL_STEPS.md) — until then it renders as
  a broken link on GitHub, which is expected and documented.
- The full live `cosmos77-rev run` is exercised end-to-end in the Phase 11 QA
  gauntlet; here it is wired + unit-tested.
